FROM python:3.9-slim

RUN apt-get update && apt-get install -y \
    docker.io \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

COPY ea_upload_server.py .

RUN pip install flask

EXPOSE 8080

CMD ["python", "ea_upload_server.py"]
